import type {ResolvedLocation} from "../lib/backend";
export function LocationDisplay({location}:{location:ResolvedLocation|null}){if(!location)return <span>—</span>;return <details><summary className="inv-name">{location.fullPath.join(" → ")}</summary><div className="ph-mission">System: {location.solarSystemName??"—"} · Constellation: {location.constellationName??"—"} · Region: {location.regionName??"—"}</div><div className="ph-mission">Raw ID: {location.rawLocationId} · {location.locationKind} · {location.resolutionStatus} · {location.resolutionSource} · Resolved: {location.lastResolved??"never"}</div></details>}
